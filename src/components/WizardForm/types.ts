// Shared types for WizardForm components.

/** WizardDict: planner dict allows dynamic key access (city_*, act*, dietaryRestriction*, price*, etc.) */
export type WizardDict = Record<string, string | undefined>;
