# Dependencies
/node_modules
/.pnp
.pnp.js

# Testing
/coverage
test-results/
tests/visual-report/
playwright-report/
tests/report/

# Production
/dist

# Misc
.DS_Store
*.pem

# Environment Variables
.env
.env*.local
# 2026-05-13 PR (Critical security fix): .env.preview.tmp, .env*.tmp 등 비밀키 파일
# 절대 push 차단. Firebase / PayPal / Gmail / Gemini / Naver / ODsay / Tripadvisor 키 노출 위험.
.env*.tmp
.env.preview*
.env*.backup

# Google Service Account (민감한 인증 파일 - 절대 커밋 금지)
service-account.json

# Local Netlify folder
.netlify

# Cache & Logs
.vercel
*.log
tmp/
android/.gradle
android/.idea
android/.cxx
android/local.properties
android/*.keystore
android/*.jks
android/app/build
android/build
android/app/build-outputs
android/app/build/outputs
android-owner/.gradle
android-owner/.idea
android-owner/.cxx
android-owner/**/local.properties
android-owner/**/keystore.properties
android-owner/**/*.keystore
android-owner/**/*.jks
android-owner/**/*.p12
android-owner/**/*.pfx
android-owner/**/keys/
android-owner/**/signing/
android-owner/app/build
android-owner/build
android-owner/app/build-outputs
android-owner/app/build/outputs

# Planner validation reports (contain API responses, not for repo)
scripts/planner-report*.json
scripts/planner-report*.txt
.env.admin.local
.vercel

# UX audit output — regenerated locally or in CI; screenshots are large, don't bloat repo
reports/
audit_*.json

# L3 quality monitor CI status file (uploaded as artifact, not committed)
quality-alert-status.json

# Ad-hoc P-cycle measurement scripts (2026-05-28 cleanup) — keep only operational tools
# (p258-dispatch / p260-fill / inspect-operator-recent / transit-cache-stats are tracked).
scripts/inspect-p[0-9]*.mjs
scripts/p245-*.mjs
scripts/p256-*.mjs
scripts/p258-measure-*.mjs
scripts/p266-*.mjs
scripts/p267-*.mjs
# P293 (2026-05-29): 5/29 cycle ad-hoc scripts.
scripts/p273-*.mjs
scripts/p280-*.mjs
scripts/p287-*.mjs
scripts/verify-p[0-9]*.mjs
scripts/pdf-golden-*.mjs
scripts/compare-5-plans*.mjs
scripts/dump-pdf-text.mjs
scripts/capture-5-plans*.mjs
# P319 cleanup (2026-05-31): 5/30~31 streaming blocker + audit cycle ad-hoc 진단 스크립트.
# 재사용 운영 도구(inspect-inngest-sync / inspect-vercel-env-inngest / inspect-preview-deploys / monitor-newest-plan)는 tracked 유지.
scripts/b3-*.mjs
scripts/b5*.mjs
scripts/fetch-preview-validation-logs.mjs
scripts/inspect-error-plan-detail.mjs
scripts/inspect-live-plan-state.mjs
scripts/inspect-streaming-plans.mjs
scripts/p[0-9][0-9][0-9]-*.mjs
.claude/worktrees/
CLAUDE.local.md
outputs/
# KTO 사진 갤러리 — 3GB/5300장 로컬 자산. 커밋하면 레포·Vercel 배포가 터진다.
public/kto-photo-gallery/
# 로컬 시각확인 스크린샷 아티팩트
tests/screenshots/mobile-home-v2-light/

# Playwright Vercel bypass storageState (CI 생성, 커밋 안 함)
tests/.auth/

# Brain guide projection repo lock (crash residue only; never content)
/.guide-projection.lock
/.guide-projection.lock.*.stale
