# CocoTrip 전체 점검 보고서 — 2026-04-20

**프로젝트**: CocoTrip (`planning-with-ai-a0801`)  
**URL**: https://cocotripkr.com  
**실행일**: 2026-04-20 17:42~17:55 KST  
**배포 커밋**: `7411a10` (main, origin/main 동기화 확인)

---

## 1. Part A 검증 결과

### A1. Bug #2 쿠폰 소진 — ⚠️ 사용자 수동 검증 필요

`capturePaypalOrder.js`에 `isUsed: true` 마킹 코드가 배포됨.  
**Test Mode는 capture 경로를 타지 않으므로** PayPal 샌드박스 실결제로만 검증 가능.

AG curl로 확인 가능한 범위:
- ✅ `applyPromoCode`가 `couponDocId`를 응답에 포함 (코드 확인됨)
- ✅ `capturePaypalOrder`가 `couponDocId`/`couponUserId`를 body에서 수신 (코드 확인됨)
- ⚠️ 실제 PayPal capture → isUsed 마킹은 **사용자 샌드박스 결제 시 확인**

### A2. OG 이미지/메타 프리뷰 — ✅ PASS

| 항목 | 결과 |
|------|------|
| `og:` 메타 태그 | ✅ 10개 (title, description, image, url, type 등) |
| `twitter:` 메타 태그 | ✅ 4개 (card, title, description, image) |
| `/api/og-image` 엔드포인트 | ✅ HTTP 200, `image/webp`, 2MB |
| OG 표준 크기 (1200×630) | ⚠️ webp 형식 — 대부분 SNS 지원하나 일부 구형 봇에서 문제 가능 |

### A3. 보안 재검증 — ✅ 12/13 PASS

| 세트 | 결과 |
|------|------|
| 기본 3케이스 | ✅ 3/3 PASS |
| 강화 10케이스 | ✅ 9/10 PASS |
| **D4 earlybird/counter** | ❌ 404 (문서 자체가 없음 — **보안 이슈 아님**, 데이터 부재) |

> D4는 earlybird/counter 문서가 Firestore에 생성되지 않아 404. 규칙은 read 허용이지만 문서가 없으므로 정상 동작. 테스트 기대값 수정 또는 문서 생성 필요 (P2).

### A4. 빌드 — ✅ PASS

| 항목 | 결과 |
|------|------|
| `tsc --noEmit` | ✅ 에러 0건 |
| `vite build` | ✅ 성공 (10.02s) |
| 번들 크기 경고 | ⚠️ 일부 청크 >500KB (기존 알려진 이슈) |

### A5. 플래너 품질 — ⏭️ 생략

D3 변경이 AI 플래너 로직에 영향 없음 (별도 모듈). 필요 시 `node scripts/validate-planner.cjs` 실행.

---

## 2. Part B Audit 요약

### B1. 기능 맵 — 6개 여정

| # | 여정 | 상태 | 비고 |
|---|------|------|------|
| J1 | 홈 → 투어 둘러보기 | ✅ 정상 | 페이지 로딩, 투어 리스트 |
| J2 | 투어 예약 + PayPal | ✅ 정상 | SDK 로드 확인 |
| J3 | AI 플래너 생성 | ✅ 정상 | 질문지 작성 가능 |
| J4 | 플랜 상세 + PDF | ✅ 정상 | |
| J5 | 플랜 공유 (D1) | ✅ 정상 | OG 태그 + earn-share 작동 |
| J6 | MyPage + 로열티 (D3) | ✅ 정상 | Redeem UI + 쿠폰 발급 확인 |

### B2. API 엔드포인트 카탈로그 — 28개 파일, 13개 공개

| Path | GET 응답 | 상태 |
|------|----------|------|
| `/api/ai-planner-full` | 405 | ✅ |
| `/api/ai-planner-quick` | 405 | ✅ |
| `/api/og-image` | 200 | ✅ |
| `/api/loyalty` | 405 | ✅ |
| `/api/applyPromoCode` | 405 | ✅ |
| `/api/createPaypalOrder` | 405 | ✅ |
| `/api/capturePaypalOrder` | 405 | ✅ |
| `/api/reserve-slot` | 405 | ✅ |
| `/api/check-availability` | 400 | ✅ (body 없이 호출) |
| `/api/plan-delete` | 405 | ✅ |
| `/api/plan-status` | 400 | ✅ (param 없음) |
| `/api/recalc-transit` | 405 | ✅ |
| `/api/chat` | 405 | ✅ |

**404 없음** — 모든 엔드포인트 배포 완료 확인.

### B3. Firestore 컬렉션

| 컬렉션 | 역할 |
|--------|------|
| `plans` | 유료/무료 플랜 (uid별) |
| `users` + 서브컬렉션 7개 | 로열티, 쿠폰, 포인트이력, 공유보상 |
| `tours` | 투어 상품 + bookings |
| `earlybird` | 얼리버드 프로모 (counter 문서 미생성) |
| `used_paypal_orders` | 결제 중복 방지 |
| `api_stats` | API 통계 (서버 전용) |
| `availability` | 예약 가능 슬롯 (서버 전용) |

### B4. 환경변수 — ⚠️ Vercel CLI 미설치

로컬에서 Vercel CLI 로그인이 안 되어 있어 `vercel env ls` 불가.  
코드 내 참조되는 필수 키:
- `GEMINI_API_KEY` ← 플래너 핵심
- `GOOGLE_SERVICE_ACCOUNT_KEY` ← Firestore Admin
- `PAYPAL_CLIENT_ID/SECRET` + Sandbox 버전 ← 결제
- `NAVER_CLIENT_ID/SECRET` ← 지도
- `ODSAY_API_KEY` ← 대중교통
- `VITE_FIREBASE_*` 6종 ← 클라이언트 Firebase
- `VITE_GA_MEASUREMENT_ID` ← GA4

> 실제 Vercel 대시보드에서 누락 확인은 사용자 필요.

### B5. 의존성 감사

| 심각도 | 건수 |
|--------|------|
| critical | **1** (protobufjs) |
| moderate | 3 |
| low | 8 |

> `protobufjs` critical — firebase-admin의 하위 의존성. `npm audit fix`로 해결 가능할 수 있으나 breaking change 위험. P1.

### B6. 코드 품질 지표

| 지표 | 수치 | 평가 |
|------|------|------|
| TODO/FIXME/HACK | **4** | 🟢 양호 |
| console.log (src/) | **7** | 🟢 양호 |
| `: any` 타입 | **133** | 🟡 높음 — 대부분 eslint-disable 포함 |

### B7. Git 상태

| 항목 | 결과 |
|------|------|
| main ↔ origin/main | ✅ 동기화 |
| 커밋 순서 | `7411a10` ← `1f3a83a` ← `2ffeb91` ← `0207494` |
| unstaged 변경 | `.gitignore`, `migrate-plans-public.mjs` (의도된 변경) |
| untracked | docs/ 7개 + scripts/ 7개 + firestore.rules 백업 3개 |

---

## 3. 발견 이슈 리스트

### 🔴 P0 (즉시 처리 요)

**없음** ✅

### 🟡 P1 (단기)

| # | 이슈 | 위치 | 설명 | 예상 |
|---|------|------|------|------|
| 1 | A1 쿠폰 소진 미검증 | `capturePaypalOrder.js` | 코드는 배포됨, 샌드박스 실결제로 검증 필요 | 20분 |
| 2 | protobufjs critical CVE | `node_modules/protobufjs` | firebase-admin 하위 의존성 | `npm audit fix` 시도 |
| 3 | 번들 >500KB 경고 | vite build | 코드 스플릿팅 또는 manualChunks 설정 | 30분 |
| 4 | `earlybird/counter` 문서 미생성 | Firestore | 얼리버드 카운터 문서 없음 (테스트 D4 실패 원인) | 5분 |

### 🟢 P2 (장기)

| # | 이슈 | 위치 | 설명 |
|---|------|------|------|
| 1 | any 타입 133건 | src/ 전체 | 점진적 타입 강화 |
| 2 | OG 이미지 webp 형식 | `/api/og-image` | PNG 대신 webp — 구형 SNS 봇 호환성 |
| 3 | untracked docs/scripts | git | 문서/스크립트 커밋 또는 .gitignore 추가 |
| 4 | 환율 API 함수 중복 | `applyPromoCode`/`createPaypalOrder` | 공통 유틸 분리 |
| 5 | 환경변수 감사 | Vercel | 대시보드에서 수동 확인 필요 |

---

## 4. 다음 세션 후보 우선순위

1. **P1-1**: 쿠폰 소진 E2E (사용자 샌드박스 결제)
2. **P1-2**: `npm audit fix` (protobufjs CVE)
3. D2 eSIM (Airalo 연동) — 신규 매출원
4. 리뷰 시스템 풀스택 — 사용자 참여 강화
5. P1-3 번들 최적화

---

## 5. 현재 프로덕션 상태 (원샷 요약)

- **배포 커밋**: `7411a10` (2026-04-20)
- **주요 기능**: 6개 여정 전부 ✅ 정상
- **보안**: 12/13 PASS (D4는 데이터 부재, 보안 이슈 아님)
- **결제**: PayPal Live/Sandbox 분기 작동, 쿠폰 소진 코드 배포됨 (실증 대기)
- **로열티**: earn-share + redeem-coupon 프로덕션 검증 완료
- **알려진 제약**: Test Mode는 capture 경로 미통과, 환율 1350 cap 적용
