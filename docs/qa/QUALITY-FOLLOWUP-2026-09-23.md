# 추가 수정·검증 기록 (2026-09-23)

🌐웹. 기준 버전 `f2a01b67` / 작업 브랜치 `codex/quality-security-followup-20260923`.
이 기록은 실제 실행한 범위의 결과다. 전체 91개 기능 정상 또는 실제 예약·결제 성공 판정이 아니다.

## 수정과 근거

| 항목 | 원인·변경 | 확인 결과 |
|---|---|---|
| 홍보 설정 중복 조회 | 공통 화면에 배너·팝업이 함께 올라오면서 같은 `/api/promo-config`를 각각 조회. 진행 중 요청과 JSON 해석만 공유하고 완료·실패 뒤 해제 | 수정 전 오프라인 production 브라우저에서 `/`, `/tours`, `/charter` 각각 2회. 수정 후 실제 dev 브라우저 세 경로·모바일에서 1회로 두 화면 표시, 콘솔/pageerror 0. 컴포넌트 검사에서 성공·실패 뒤 재조회 확인. 영구 캐시는 추가하지 않음 |
| 의존성 취약 항목 | 기존 허용 버전 범위에서 잠금 파일만 최소 갱신. manifest·override·선택 의존성 보존 | 독립 `npm ci --ignore-scripts` 통과. 전체 audit 높은 위험 4→0, 중간 위험 19→12, 낮은 위험 1→0. 운영 의존성은 중간 위험 13→8, 높은/치명적 0 유지 |
| 메일·엑셀·PDF 회귀 | Nodemailer·fflate 변경 영향 검사 | 메일 실제 MIME 조립 13개, 엑셀 11개(실제 XLSX 압축 생성·4개 시트 복원 포함), audit 판정 11개: 35개 통과. 같은 합성 일정의 실제 상세 화면→PDF 다운로드 1개 통과(2쪽, 265,270바이트, pageerror 0); 모바일 가로 넘침 없음 |
| 투어 예약 버튼 연속 클릭 | React가 로딩 상태를 화면에 반영하기 전 두 클릭이 들어오면 공통 PayPal 버튼에서 주문 요청 2개 생성 | 실제 달력·입력 폼·요청 생성 코드를 실행해 재현. 동기식 진행 중 가드 제안본은 동일 클릭 1회 및 503 뒤 재시도 통과. 결제·예약 별도 승인 경계에 따라 제품 적용 보류 |
| 야간 투어 표시가·서버가 불일치 | `tour-seoul-night`는 인당 가격으로 표시하지만 서버에는 `charter_seoul_city`라는 그룹 가격 상품을 전달. 화면 기대 USD도 요청에서 누락 | 실제 UI→실제 `createPaypalOrder.js` 합성 연결 검사 1개 통과: 2명 표시 ₩140,140, 서버 snapshot ₩330,000, PayPal 주문 생성 body $236.00. 기존 `AMOUNT_MISMATCH` 검증은 기대 금액이 없어 실행되지 않음. 실제 결제 없음; 가격 계약 수정·불일치 차단은 별도 승인 대상 |

홍보 요청 감소는 해당 HTTP 조회 수의 감소다. Gemini 호출·전체 비용이 같은 비율로 줄었다는 뜻은 아니다.

## 보안 항목이 남는 이유

audit의 12개는 서로 다른 결함 12개가 아니라 아래 2개 권고가 상위 의존성에 전파된 패키지 기록이다. 예외를 추가하거나 검사 기준을 낮추지 않았다.

- 운영 8개: Firebase Admin 하위 UUID. [공식 권고](https://github.com/advisories/GHSA-w5hq-g745-h8pq)는 사용자 버퍼·offset을 받는 v3/v5/v6의 범위 검증 문제다. 설치된 gaxios·teeny-request·google-gax에서 확인한 사용은 v4이므로 해당 취약 호출을 발견하지 못했다. 취약 버전이 설치돼 있다는 사실은 남는다. 자동 수정안은 Firebase Admin 14 계열로 큰 버전 변경이므로 인증·저장 호환 검증을 거쳐 별도 적용해야 한다.
- 개발 4개: 프리렌더러 하위 ts-deepmerge. [공식 권고](https://github.com/advisories/GHSA-87mf-gv2c-c62c)는 신뢰할 수 없는 객체의 `toString`/`valueOf` 덮어쓰기 문제다. 확인한 호출은 Vite 프리렌더 빌드 설정 병합이다. 현재 부모 패키지는 취약한 6 계열을 사용하며, 8 계열 강제 지정 또는 프리렌더러 교체는 별도 호환 검증이 필요하다.

## Lighthouse 판정

- 실제 저장소 Vite 설정, 빈 환경파일 디렉터리, 합성 Firebase 공개 설정으로 scratch production 번들을 만들었다. 외부 HTTP·WebSocket·서비스워커를 차단하고 API 경계만 대체했다.
- 고정된 LHCI 0.15.1 / Lighthouse 12.6.1로 세 경로 검사: SEO 모두 1.00, 콘솔 오류·이벤트 리스너·강제 레이아웃 감사 통과. 기존 원격 SEO 경고는 로컬에서 재현되지 않아 메타 정보를 임의로 바꾸지 않았다.
- `network-dependency-tree-insight`는 세 경로에서 0. 문서→JS/CSS/manifest의 필수 연결도 2단계면 실패로 판정하는 해당 감사의 동작을 확인했다. 로컬 최장 연결은 121~133ms였다. 검사 정책과 서비스 목표를 따로 검토해야 하며, 이번에는 기준을 완화하지 않았다.
- 사용하지 않은 JS/CSS 경고는 남는다. 가장 큰 JS 후보는 초기 Firestore(약 100KB 절감 추정)다. 초기 Sentry도 포함되지만 SDK별 정확한 기여량과 운영 DSN 조건을 분리 측정하지 못해 추측으로 로딩·오류 수집 구조를 바꾸지 않았다.
- 이전 로컬 `dist`는 키 없는 검사용 빌드여서 브라우저 렌더가 불가능했다. 그 `NO_FCP`는 제품 장애 근거에서 제외하고 합성 설정의 별도 production 번들로 다시 측정했다.

## 남은 경계와 검사 운영

- 가격 불일치 재현 조건: 합성 날짜 2030-01-05, 2명, 할인 v2 기본 OFF, 쿠폰·프로모션·옵션 없음, 외부 인증/DB/환율/PayPal 통신 대체. UI·상품 데이터·서버 가격 계산은 실제 코드다. 인당/그룹 가격 중 어느 것이 정답인지 운영 정책을 정한 후 상품별 계약을 맞춰야 한다.
- 투어가 선택 회차를 메모로 보내면서 `pickupTime`은 문서화된 기본값 09:00으로 보내는 것을 확인했다. 이를 18:00으로 바꾸는 것만으로 가격 불일치는 해결되지 않는다. 현재 금액·시간·상품 매핑은 변경하지 않았다.
- 실제 PayPal 주문·결제·발송·운영 DB·유료 모델 호출은 하지 않았다. mock 응답은 실제 저장·발송 성공 증거가 아니다.
- 구현·검사: GPT-6 Luna 3개 작업(의존성/홍보, 성능 원인, 투어 재현). 주 담당은 범위·돈 안전·최종 검수와 통합 검사를 수행했다.
- 수정 중에는 관련 검사만 실행한다. 최종 정상 push 훅의 전체 build·unit·size·오답패턴·React 훅 검사는 한 번 실행하며, 결과와 PR·배포 상태는 종료 보고에 추가한다.

로컬 상세 근거: `tmp/quality-followup-20260923/` (합성 자료·스크린샷·감사 JSON·제안 diff; git 제외). 바탕화면 보고서는 생성하지 않았다.
