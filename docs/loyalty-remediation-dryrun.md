# 충성도 원장 오염 보정 — dry-run 보고서

> 읽기 전용 실행 결과. 운영 데이터는 **수정하지 않았다.**
> 개인정보(uid·이메일·예약번호·PayPal ID)는 담지 않는다. 계정은 순번으로만 표시.

- 계획 해시(planHash): `98b493fabb8b45e2`
- Firestore 읽기: 26회 / **쓰기 시도: 0회 / 실제 쓰기: 0회**
- 쓰기 차단 방식: dry-run 에서는 set/update/delete/add/batch/runTransaction 호출이 예외를 던진다

## 전체 합계

| 항목 | 수정 전 | 수정 후 | 차이 |
|---|---:|---:|---:|
| 누적 지출(USD) | $47,996.81 | $0 | -47,996.81 |
| 예약 수 | 59 | 0 | -59 |
| 코인 | 140,642 | 0 | -140,642 |

- 보정 대상 계정: **2** (스캔 8)
- 오염 적립 이력: 207건 / 코인 626,388 / 지출 $208,794.68
- 실행 시 바뀔 문서 수: **6**
  (복구 스냅샷 2 + 사용자 2 + correction 원장 2 + 회수 쿠폰 0)

## 🔴 보정 방식 (계정별)

| 방식 | 계정 수 | 뜻 |
|---|---:|---|
| `subtract_pollution_only` | 2 | 명백한 오염분만 차감. 나머지는 손대지 않음 |
| `recompute_from_ledger` | 0 | 결제 원장으로 전부 재계산(원장이 완전할 때만) |

### 결제 원장 귀속 현황

- bookings 총 29건 중 **uid 필드가 있는 것 0건**, `paymentVerified===true` 0건
- 계정에 귀속 불가: **29건**
- uid·paymentVerified 는 2026-07-29 PR 에서 추가된 필드다. 그 이전 결제 문서에는 없어 계정에 귀속되지 않는다.
- 그래서 이번 보정은 **전체 재계산을 쓰지 않는다.** 근거가 확실한 오염분만 뺀다.

- 보정 후 잔액 중 **검증된 결제로 뒷받침되는 금액: $0**
- 보정 후 잔액 중 **근거를 붙이지 못한 금액: $0** (건드리지 않음)

## 정상으로 보존할 데이터

- 검증된 유료 주문: 0건
- 정상 적립 코인: 0
- 기존 pointHistory 는 **삭제하지 않는다.** 역분개(correction) 항목만 추가한다.

## 애매하여 보류한 데이터 (자동 수정 안 함)

- 결제 근거가 불완전한 적립 이력: 3건
- **계정 내부** 애매 주문(통화·금액 불완전): 0건
- **전역 귀속 불가 레거시 주문: 29건** — 계정에 붙지 않아 검증 자체가 불가능하다. "애매 주문 0건" 과 별개 항목이다.
- 출처 불명 쿠폰: 0장

## 쿠폰 처리 계획

- 회수(사용 불가 전환) 대상 **미사용** 오염 쿠폰: 0장 — 삭제하지 않고 `revoked` 상태로 변경
- **이미 사용된** 오염 쿠폰: 0장 — 회수·재청구 없음, `grandfathered` 감사 표시만
- 이미 제공된 할인 규모(사용된 오염 쿠폰 할인율 합): 0%p
- 이미 소진된 오염 코인(재청구하지 않음): 2,000

## 계정별 (익명 순번)

| # | 지출 전→후 | 예약 전→후 | 코인 전→후 | 등급 전→후 | 오염 이력 | 보류 | 쿠폰 회수/유지 | 문서 |
|---|---|---|---|---|---:|---:|---|---:|
| user-1 | $47,279.42 → $0 | 58 → 0 | 139,207 → 0 | Platinum → Bronze | 202 | 3 | 0/0 | 3 |
| user-2 | $717.39 → $0 | 1 → 0 | 1,435 → 0 | Gold → Bronze | 5 | 0 | 0/0 | 3 |

## 실행 방법 (별도 승인 후)

```
LOYALTY_REMEDIATION_APPROVAL=I-APPROVE-LOYALTY-WRITES \
node scripts/loyalty-remediation.mjs --execute --confirm=98b493fabb8b45e2
```
- 셋(`--execute` + `--confirm` + env)이 모두 없으면 실행되지 않는다.
- 실행 직전 dry-run 을 다시 돌려 planHash 가 다르면 중단한다.
- 되돌리기: `node scripts/loyalty-remediation-rollback.mjs --confirm=<runId>` (별도 승인 필요)
