# 04 — 수정 후 post-execution dry-run (현재 사실)

> 이 문서가 **현재 상태**다. `transient-bug-reproduction-dryrun.*` 은 결함이 남아 있던 시점의 자료다.

시점: 2026-07-29, [03 결함](03-double-subtraction-defect.md) 수정 후 · 읽기 전용
개인정보 없음. 계정은 순번만.

## 용어 (FAIL-17)

"오염 이력 0건" 이라고 쓰지 않는다. 오염 이력(`pointHistory`)은 **감사 목적상 삭제하지 않으므로**
보정 뒤에도 그대로 남는다. 셋을 나눠 쓴다.

| 용어 | 뜻 |
|---|---|
| **역사적 오염 이력** | 과거에 잘못 적립된 기록. 지워지지 않는다 |
| **이미 제거된 몫** | 인정된 correction 이 이미 차감한 금액·코인 |
| **남은 미보정 오염** | 아직 안 뺀 몫. **이 값이 0 이어야 수렴한 것** |

## 결과

```
보정 대상 계정   : 0        ← 오염이 없었다는 뜻이 아니다
실행 시 문서 수  : 0
계획 해시        : e3b0c44298fc1c14   (빈 계획)
Firestore        : 읽기 25 / 쓰기 시도 0 / 실제 쓰기 0

역사적 오염 이력 : 209건 (코인 629,237 / 지출 $210,219.21)
이미 제거된 몫   : 209건 (코인 629,237 / 지출 $210,219.21) · 인정 correction 4건
남은 미보정 오염 : 0건   (코인 0 / 지출 $0)
인정 안 된 correction : 0건 · manual_review 계정 0
```

## 현재 전체 합계 (보정 후 값)

대상이 0 건이어도 이 값은 반드시 나온다.

| 항목 | 값 |
|---|---:|
| 누적 지출 | $47,996.81 |
| 예약 수 | 59 |
| 코인 | 140,642 |

## 계정별 (익명 순번)

순번은 **보정으로 바뀌지 않는 값**(오염 코인 → 오염 금액 → 오염 건수)으로 매긴다.
그래서 [01 사전](01-pre-execution-dryrun.md) · [02 실행](02-execution-result.md) · 이 문서에서
같은 사람이 **같은 user-N** 이다.

| # | 지출 | 예약 | 코인 | 등급 | 역사적 오염 | 인정/보류 correction | 남은 오염 | 추가 보정 |
|---|---:|---:|---:|---|---:|---|---:|---|
| user-1 | $47,279.42 | 58 | 139,207 | Platinum | 202 | 1 / 0 | $0 | 아니오 |
| user-2 | $717.39 | 1 | 1,435 | Gold | 5 | 1 / 0 | $0 | 아니오 |
| user-3 | $0 | 0 | 0 | Bronze | 1 | 1 / 0 | $0 | 아니오 |
| user-4 | $0 | 0 | 0 | Bronze | 1 | 1 / 0 | $0 | 아니오 |

`correction 인정 4 / 보류 0` — 운영에 생성된 레거시 correction 4건이 엄격 판별을 전부 통과했다.

> ⚠️ **이전 판 정정**: 이전 문서는 계정 번호를 Firestore 문서 ID 순서로 붙여
> 01·02 와 어긋나 있었다(같은 사람이 문서마다 다른 번호). 위 순번 규칙(FAIL-17)으로 다시 맞췄다.
> 값 자체는 2026-07-29 읽기 전용 확인 결과 그대로이고 바뀌지 않았다.

## 남은 금액에 대한 정직한 표시

보정 후 잔액 **$47,996.81 중 검증된 결제로 뒷받침되는 금액은 $0** 이다.

이유: 운영 `bookings` 29건 전부 `uid` 필드가 없어 계정에 귀속되지 않는다.
그 필드는 같은 날 PR #1187 에서 추가됐고, 그 이전 결제 문서에는 존재하지 않는다.

**근거 없이 차감하지 않고 그대로 보류한다.** 이 잔액을 더 줄일지는 별도 판단이 필요하다.

| 보류 항목 | 수량 |
|---|---:|
| 결제 근거가 불완전한 적립 이력 | 3건 |
| 계정 내부 애매 주문 | 0건 |
| **전역 귀속 불가 레거시 주문** | **29건** |
| 출처 불명 쿠폰 | 0장 |
| 인정되지 않은 correction | 0건 |
| manual_review 로 분리된 계정 | 0계정 |

## 다음 dry-run 부터 출력 형식이 달라진다

FAIL-17 수정으로 보고서 합계가 두 범위로 갈렸다.

- `totals.allScanned` — 위 "현재 전체 합계"·"역사적/남은 오염" 같은 **현재 사실**
- `totals.plannedTargets` — **이번 실행이 바꿀 것** (대상 수·전후·차이·문서 수)

숫자는 같고, 한 덩어리로 섞여 있던 것이 나뉜 것이다.
