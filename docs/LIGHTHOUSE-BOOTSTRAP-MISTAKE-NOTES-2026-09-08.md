# 보호된 미리보기 검사 도구 최초 설치

## 2026-09-24 보호된 설치 설정 파일의 쿠키 누락

- main의 진단 수정(PR1418) 후 검사 35969794949 첫 실행은 수집 단계에서 중단됐다. 한 번 재실행하자 세 경로 수집이 완료됐고, 각 경로에서 CORS 2건·리소스 로드 오류 2건이 확인됐다. HTTP 400–599 요청은 없었다. 최초 수집 실패를 페이지 결함으로 계산하거나 재시도 성공을 정책 통과로 보고하지 않는다.
- 현재 빌드가 만드는 `<link rel="manifest">`에는 쿠키 전달 설정이 없었다. 실제 Chromium과 루프백 서버 두 개로 재현한 결과, 기존 태그는 이미 가진 HttpOnly 쿠키를 전달하지 않아 인증 화면으로 리디렉션되고 CORS 오류가 발생했다. 같은 태그에 `crossorigin="use-credentials"`를 적용하면 쿠키가 전달되고 리디렉션·CORS 오류가 모두 사라졌다. 실제 운영 인증값이나 고객 데이터는 사용하지 않았다.
- 이미 설치된 VitePWA의 `useCredentials: true` 옵션으로 빌드 태그를 수정한다. CocoTrip·MOOD·오너 설정 파일 경로는 모두 같은 출처의 고정 경로이며 기존 ManifestSwitcher는 해당 속성을 보존한다. 인증 우회, CORS 허용 범위 확대, 미리보기 보호 해제, 쿠키·키 발급은 하지 않는다. 근거: [Vite PWA 공식 FAQ](https://vite-pwa-org.netlify.app/guide/faq#web-app-manifest-and-401-status-code-unauthorized).
- 스크롤 경고는 세 경로 모두 미리보기와 같은 출처 1건으로 확인됐지만 정확한 함수는 미확정이다. 같은 출처라는 사실만으로 제품 코드와 미리보기에 주입된 스크립트를 구분할 수 없다. 직접 등록한 PWA 입력 감지와 React의 스크롤 관련 이벤트는 이미 passive를 사용하므로 추측으로 변경하지 않는다.
- 미사용 CSS·JS, 요청 의존성, Preview 색인 차단, 변동하는 출처 미상 리플로우와 미분류 판정은 별도 잔여 항목이다. 로컬 쿠키 재현 통과가 원격 CORS 전체 해소나 실제 설치·업데이트·문의 저장·결제 성공을 증명하지 않는다. 원격 적용 후 동일 검사로 따로 대조한다.
- 회귀 검사는 `npm run build` 후 `npx playwright test tests/e2e/manifest-cookie-credentials.spec.ts --project="Desktop Chrome" --workers=1 --reporter=line`으로 실행한다. 실제 생성된 HTML 태그를 사용하고 쿠키 누락 대조군의 실패와 수정군의 성공을 한 검사에서 확인했다(1개 통과). 모든 요청은 루프백 두 출처로 제한한다. 기존 PWA 검사 38개와 격리 dev 홈·MOOD 화면/인트로 자산도 통과했다.

오답노트: 인증 쿠키가 브라우저에 있어도 manifest 요청은 기본적으로 쿠키를 보내지 않는다. 보호를 끄는 대신 정상 요청에 필요한 속성을 사용한다. 검사 장치도 화면의 숨겨진 첫 링크나 임의 글자 수가 아니라 해당 화면의 실제 표시 요소를 기준으로 판정한다. 루프백 전용 E2E도 공용 analytics-guard의 test/context를 사용한다. 최초 푸시 전 P272가 직접 import를 차단했고, 공용 도구를 실제로 사용하도록 수정한 뒤 브라우저 재검사와 오답 린트가 통과했다.

## 2026-09-24 스크롤 경고 출처 판독 수정

- 고정 Lighthouse 12.6.1의 `uses-passive-event-listeners`는 출처를 `details.items[].source.url`에 저장한다. 기존 요약기는 이 필드를 읽지 않아 실제 출처가 있어도 `unknown`으로 분류했다. 설치된 audit 구현과 같은 구조의 회귀 검사로 수정했다.
- 콘솔 오류는 고정 오류 종류만, 실패 요청은 제한된 출처·요청 종류·HTTP 오류 상태와 개수만 요약한다. 원문 메시지·URL·헤더·쿠키를 출력하지 않고 기존 판정·보호 규칙을 유지한다. 이 수정은 실패 원인을 읽기 위한 것이며 제품 오류 해결이나 점수 통과를 뜻하지 않는다.
- 운영 배포의 실제 정적 HTML·JS를 새 브라우저에서 실행하고 API를 가짜 응답으로 바꿔 세 경로를 확인했다. 인증·DB·분석·지도·결제·발송 요청과 WebSocket은 통과시키지 않았다. 콘솔 오류·실패 요청·예외는 없었고, Lighthouse와 같은 CDP 위반 수집에서도 경고가 재현되지 않았다. 서비스워커 차단에 따른 검사 장치 경고는 제품 결함으로 계산하지 않는다.
- Vercel 연결 도구는 보호된 미리보기 접근을 거부했다. 연결 권한 갱신을 요청하면서 기존의 검토된 main 도구를 사용하는 검사 경로를 유지한다. 원문 오류가 없는 상태에서 콘솔 오류를 특정 API나 Vercel 도구 모음 탓으로 단정하지 않는다.

오답노트: 보고서의 실제 항목 구조를 먼저 확인한다. `sourceLocation.url`과 `source.url`은 서로 다른 audit의 필드이며, 출처를 읽지 못한 상태를 출처가 없다는 증거로 사용하지 않는다.

## 2026-09-24 초기 로딩 수정과 원인 진단 보강

- 비활성 Sentry의 수동 오류 보고 함수에도 운영 모드·DSN 조건을 적용해 빌드가 불필요한 SDK 코드를 제거하게 했다. 활성 상태에서는 기존 동기 초기화를 유지하고, 기존 오류 필터·개인정보 마스킹을 보존한다. 초기화·보고 실패가 앱으로 전파되지 않고 초기화 전 보고가 실행되지 않도록 확인했다.
- 최초 지연 로딩안은 실제 Preview에서 SDK 전체를 추가 청크로 묶어 용량을 늘렸다. 머지 전에 합성 DSN을 켠 빌드로 재현해 조건문 방식으로 줄였다. 같은 활성 설정에서 JS 합계 6,418,162 → 6,021,822바이트, gzip 합계 2,071,444 → 1,941,678바이트. 동일한 합성 Firebase 설정·PRERENDER=0·DSN 없음으로 기존 main Sentry 코드를 주입해 다시 대조한 초기 entry gzip은 114,211 → 110,338바이트로 3,873바이트 감소했다. 앞선 서로 다른 합성 설정의 수치는 이 대조값으로 정정한다. 활성·비활성 설정을 모두 확인해야 하는 사례다.
- 같은 합성 DSN·설정에서 기존 main Sentry 코드를 주입한 대조 빌드도 확인했다. 활성 JS 합계는 기존 6,021,757바이트/gzip 1,941,619바이트에서 최종 6,021,822바이트/gzip 1,941,678바이트로 조건문 비용만큼 증가했다(65바이트/gzip 59바이트). 482,709바이트 추가 SDK 청크는 최종안에 없다.
- 앞선 로컬 지연 로딩안으로 `/`, `/tours`, `/charter`를 측정했을 때 성능 98/99/98, 접근성·모범 사례·SEO 100, 콘솔·페이지 오류와 외부 요청 0건이었다. 이 점수를 최종 조건문 방식이나 운영 성능의 측정값으로 대신하지 않는다.
- 강제 리플로우는 수정 전 `/charter`에서 출처 없는 70.676ms가 관측됐고 수정 후 세 경로에서 통과했다. 별도 브라우저 추적에서는 호출 원인을 특정하지 못했으므로 **Sentry 수정으로 리플로우를 해결했다고 판단하지 않는다.** 미사용 CSS·JS와 요청 의존성 검사는 여전히 실패하며 기존 assert 기준은 유지했다.
- 원격 실패의 원문 보고서는 인증 정보 노출 방지를 위해 계속 공개하지 않는다. 기존 검증기에 고정 경로, 오류·리스너 출처 분류별 개수, 색인 차단 여부, 리플로우 시간·출처 유무만 출력하는 제한된 진단을 추가했다. 최상위 호출 표와 하위 합산 표를 중복 계산하지 않는다. 진단은 판정 결과나 종료 코드를 바꾸지 않는다.
- 이 진단은 검토 후 main에 들어가야 원격 검사에 사용된다. 현재 로컬 검증만으로 이전 Preview의 콘솔·리스너 오류 원인이나 운영 문의·로그인·결제 성공을 주장하지 않는다. 실제 발송·DB 접근·유료 API 호출 없이 검사했다.
- 회귀: 최종 Sentry 조건문 5개, Lighthouse 검증기 87개 테스트와 ESLint. 최초안은 실제 `npm run build`, 세 경로 브라우저·고정 버전 LHCI 검사도 실행했다. 최종안은 활성·비활성 합성 빌드를 별도로 대조했다. 외부 HTTP·WebSocket 차단 아래 데스크톱 세 경로와 모바일 /charter의 실제 렌더링·화면을 확인했고 콘솔·페이지 오류, 실패 요청, 외부 요청, 가로 넘침은 모두 없었다. 정상 pre-push 5종 검사는 185초에 통과했다(869파일/11,876통과·기존 TODO1). 작업은 GPT-6 Luna가 제한된 두 구현을 맡고 주 담당이 호출 경로, 진단 계산, 테스트 격리 및 빌드·화면을 검수했다.

오답노트: SDK 초기화만 막고 수동 보고 경로를 남기면 비활성 빌드에도 코드가 남는다. 반대로 동적 import의 전체 namespace를 반환하면 사용하지 않는 SDK 기능까지 묶일 수 있다. 실제 활성·비활성 빌드로 대조한다. 출처 없는 리플로우와 일회 통과를 근거로 특정 앱 코드의 결함·해결을 단정하지 않는다. 진단 데이터는 실제 Lighthouse 스키마로 검증하고 점수 정책과 분리한다.

## 2026-09-21 배포 후 로컬 원인 분리

- CI와 같은 고정 treosh 배포본의 LHCI 0.15.1/Lighthouse 12.6.1로 기존 합성 환경 빌드의 `/`, `/tours`, `/charter`를 측정했다. 외부 HTTP/WebSocket 차단, Chrome DNS 제한, 서비스워커 차단, 가상 API 응답을 사용했다. 운영 또는 같은 조건의 미리보기 측정으로 간주하지 않는다.
- 최종 로컬 성능 점수는 82/83/81, 접근성·SEO는 세 경로 모두 100, `forced-reflow-insight`는 모두 통과했다. 이전 미리보기의 추가 강제 레이아웃 실패가 회귀인지 측정 변동인지는 여전히 미확정이다.
- 초기 검사 장치의 `default-src 'self'`는 내부 script/style까지 막아 60개 오류를 만들었다. 외부 통신 차단을 유지하면서 내부 스타일·스크립트를 허용한 최종 실행의 console 오류 16개는 모두 `worker-src 'none'`에 의한 SW 등록 차단이었다. 이 오류를 제품 결함으로 수정하거나 숨기지 않는다.
- 미사용 CSS/JS 등 기존 성능 부담은 남아 있고 assert 기준을 낮추지 않았다. 루프백 서버의 캐시·압축 설정과 가상 API·SW 차단도 수치에 영향을 주므로, 로컬 점수로 운영 개선 완료를 보고하지 않는다.

## 승인과 범위

운영자가 2026-09-08 "나머지 목표까지 진행해"로 직전 제안한 도구 전용 선행 PR/최초 설치·머지 절차를 승인했다. PR #1380의 검토된 검사 도구 6파일과 이 기록만 main에 먼저 적용한다. 제품의 src/api/public, Firebase/결제/권한 규칙, 패키지, 고객 자료, 환경변수 값은 변경하지 않는다. 기존 Lighthouse assert 정책도 그대로다.

## 원인과 해결 순서

- 검사는 main의 신뢰한 도구만 실행하도록 설계했으나 첫 PR은 그 도구를 아직 main에 설치하기 전이다. 실제 run34135716421은 LHCI_TRUSTED_HELPER_NOT_ON_MAIN으로 수집 전에 실패했다. 이를 앱 성능 측정 결과나 통과로 세지 않는다.
- 이 선행 PR에 한해 최초 설치 전의 검사 미실행 상태를 명시하고, 다른 필수 검사·빌드·독립 검토를 통과한 뒤 승인된 정상 머지 절차로 도구를 설치한다. 관리자 강제 머지, 보호 설정 변경, 검사 실패 숨김은 하지 않는다.
- 설치 후 현재 제품 PR head와 SHA가 정확히 일치하는 READY Preview의 숫자 Deployment ID로 main의 검사를 실행한다. 테스트만 추가해도 head가 바뀌면 이전 ID는 거부된다. 같은 앱 URL을 재사용할 수 있는 visual 검사와 혼동하지 않는다.
- 인증/실제 세 경로 수집 성공과 사이트의 필수 성능 판정은 별개다. 기존 알려진 성능 항목이 실패하면 실패로 보고하며 기준을 낮추지 않는다.

## 보안과 비용

기존 GitHub Secrets의 VERCEL_AUTOMATION_BYPASS_SECRET은 지정 호스트 /robots.txt의 Node 요청에만 전달한다. 리디렉션을 따라가지 않고, 브라우저에는 해당 호스트의 Secure/HttpOnly 쿠키만 설치한다. PR 코드를 CI 도구로 실행하거나 원문 로그/보고서를 공개 저장소로 올리지 않는다. 키의 조회·발급·변경, 실제 고객 조회·메시지 발송은 없다. 배포 파일이 아닌 도구/검사 변경만 묶어 한 번 푸시한다.

## 회귀 확인

2026-09-08 로컬에서 두 스크립트 구문 검사, 변경한 JS/TS 4파일 ESLint, 전용 2파일 116개 테스트를 실제 실행해 통과했다. 독립 검토자도 같은 116개 테스트와 도구 6파일을 별도로 검토했다. Windows 줄바꿈만 정규화해 비교한 도구 6파일은 검토된 be5f3825와 동일하고, origin/main의 Lighthouse assert 설정과도 깊은 비교로 동일함을 확인했다. 전체 빌드·전체 단위 테스트·용량·오답 패턴·훅 순서 검사는 정상 푸시 전 훅에서 추가 실행한다.

동일 저장소/정확한 PR head/비정상 출처 거부, 비밀값을 포함할 수 있는 오류 문자열 비노출, 쿠키 범위/정리, 누락·중복·리디렉션·수집 오류·필수 판정 실패 거부를 가짜 브라우저와 보고서로 검사한다. 실제 인증은 도구 설치 뒤 별도로 확인해야 한다.

오답노트: 신뢰 기준을 main으로 정한 도구는 최초 설치 순서도 분리해야 한다. 테스트-only의 Vercel 성공 표시나 자동 통과를 실제 측정으로 대신하지 않는다. 숫자를 녹색으로 만들기 위해 검증기를 느슨하게 하지 않는다. 실패 이유가 필요해도 URL·원문 로그·전체 보고서를 공개하지 말고, 검토된 고정 정책 audit ID·유한 숫자·상한 있는 누락 건수만 안전 요약으로 남긴다.
