# 개인용 운영센터 남은 목표: 2026-09-08 후속

대상은 웹 cocotripkr.com과 개인 Android 컨트롤러다. 사용자 "나머지 목표까지 진행해"에 따라 승인된 검사 도구 최초 설치와 기존 기능·디자인 개선을 이어갔다. 아래 실제 결과와 미완료 항목을 구분한다. 전체 통합 운영 완료 보고가 아니다.

## 검사 도구 선행 설치 완료

- 별도 작업 폴더 `E:\cocotrip-lighthouse-bootstrap-20260908`, 브랜치 `codex/lighthouse-trusted-bootstrap-20260908`에서 검토된 도구 6파일과 설치 기록만 반영했다. 제품 src/api/public/Android/패키지/권한 규칙의 diff는 없다.
- PR #1381, head `fbc823d0e02355c01f5c3cc6b2435c58d4e808bf`, 2026-09-08 01:33:05 KST 정상 squash MERGED. main `df27b1a2ce11e3ea415f12dd106a81b4f8f7aac2`. 정상 머지 요청의 일시적 HTTP 502 후 PR OPEN/main 불변을 재조회하고 같은 exact-head 조건으로 재시도했다. 관리자 강제 머지/보호 변경 없음.
- 로컬 정상 pre-push 5단계 219초 PASS: 전체 build, 754파일 9,931 PASS/7 todo, 용량, 실수 패턴, 훅 순서. 원격 실제 unit34142402476은 754파일 9,929 PASS/4 skipped/7 todo 및 네 언어 2,214키 PASS.
- 도구 PR Preview `dpl_9jfZfZKXyBWg8BHdkb1veGpahEsr` READY. 실제 visual34143224629, PDF34143224741, smoke34143224662 PASS. 단순 PR-event 자동 통과와 별개다.
- 최초 Lighthouse34143224695는 아직 main에 helper가 없는 정상적인 최초 설치 순서 문제로 수집 전 실패했다. 사용자 승인한 최초 설치 예외만 적용했고 성능 실패를 통과로 바꾸지 않았다.
- 설치 후 main의 수동 Lighthouse34143864226에 정확히 일치하는 기존 Preview Deployment6312622610을 지정했다. 신뢰한 배포 검증·인증·3경로 수집 모두 실제 성공. 최종 코드는 `LIGHTHOUSE_PREVIEW_FAIL REQUIRED_ASSERTION_FAILED`뿐이며 페이지 이동/누락/형식/실행 오류 코드는 없었다. **이는 기존 운영 앱의 성능 판정 실패이며, 인증 도구 실패나 PR #1380 개선본의 성능 결과가 아니다.** 보고서·비밀값 공개 업로드 없음.

## 이번 안전한 표시 개선

- `partialErrors`와 `sources.ok=false`를 합쳐 출처별 실패를 구분한다. 실패에 영향받는 0건은 "확인 불가", 남은 양수는 원래 값과 "일부 자료 기준"을 표시한다. 예약·문의·업무 빈 상태와 상단 부분 갱신도 같은 의미다.
- 성공한 예약 원본까지 메일 조회 실패 때문에 모두 불명확 처리하지 않는다. 원래 성공 자료·링크·필터·더보기와 요청/900ms 중복 방지/최초 진입·전면 복귀 갱신 코드가 그대로다. 자동 호출 주기를 추가하지 않았다.
- 수신 메일·WhatsApp·API/호스팅 비용은 접히지 않는 별도 영역에서 "화면 미연동"으로 표시한다. 존재하지 않는 0원·잔액·최종 수신 시각을 만들지 않는다. "모두 응답"은 연결된 조회 대상에만 한정한다.
- 메일 발신 재시도의 `ok`는 "대기 없음"으로 표시한다. 실행 성공/수신함 연결/휴대폰 수신 증명과 구분한다. 전체 집계 API·인증·금액·알림 발송은 변경하지 않았다.
- 네 언어의 집중 4파일 88/88 PASS, 대상 ESLint/diff PASS. 독립 검토자도 같은 88개를 실제 재실행했고 호출 코드 동일·관리자 링크 집합 차이0을 확인했다.
- 실제 Chrome의 390/1280px × 네 언어 × 정상/부분 빈 자료/source flag-only/메일 대기 없음/갱신 실패 = 40개 화면 상태 PASS. 출처를 펼치는 클릭·키보드 초점·44px 높이·가로 넘침0 확인. 전부 개발용 가짜 자료이며 실제 API/외부 전송0이다. 증거는 `tmp/ops-status-visual-20260908`, 실행기는 `tmp/verify-ops-status-visual.mjs`다.

## 초기 CSS와 관리 화면 누름 영역

- 기존 index.css의 커뮤니티 전용 35,900바이트를 원문 그대로 지연 로딩되는 editorial-community.css 맨 앞에 이동했다. 기존 기본/편집 스타일 해시와 순서, 공개 목록·상세·작성·관리자 모두 같은 모듈에서 받는 계약을 잠갔다. 원본 CSS 크기 절감을 압축 전송량 절감으로 바꾸어 보고하지 않는다.
- 실제 PC 비교에서 `.community-icon-button.md:hidden`의 뒤로 버튼이 새로 나타나는 회귀를 발견했다. 지연 CSS가 Tailwind 유틸리티 뒤에 와 `display:flex`가 이긴 것이 원인이다. 설치된 Tailwind의 md=768px를 확인하고 해당 커뮤니티 헤더 버튼만 기존처럼 숨기는 호환 규칙으로 복구했다. 기준 화면을 새 오류에 맞춰 갱신하지 않았다.
- 복구 뒤 공개 목록/상세/비로그인 작성 390/1280px × 네 언어 24개는 이전의 실제 계산 스타일·치수와 일치했다. 작성/관리자 DEV 16조합도 별도로 비교했다. API fixture는 실제 `ok/data` 응답 계약을 사용하며 모든 쓰기·외부 호출을 차단했다.
- 관리 화면의 기존 로고36px·주요 버튼38px는 이관 전에도 미달이었다. 사용자 승인된 편의 개선으로 moderation 범위 3개 선택자에 min-width/min-height44px만 추가했다. 아이콘/SVG 크기·글꼴·색·텍스트·처리 함수는 그대로다.
- 관리자 8조합의 기존 미달 6요소 모두44px 이상/초점 누락0/가로 넘침0. 공개 작성 8조합은 기존과 완전히 일치. 증거 `tmp/community-moderation-touch-20260908`, 이전 원형 비교는 `tmp/community-css-dev-final-20260908`에 별도 보존했다. 게시/승인/숨기기/처리 완료 버튼은 누르지 않았다.
- 최종 CSS 관련 집중 6파일40개/ESLint PASS. 최종 npm run build PASS. 전체 768파일10,307 PASS/7 todo 실행은 마지막 두 CSS 호환/터치 조건 보강 이전 검사의 수치이므로, 최종 전체 결과는 정상 pre-push와 PR 기록에서 다시 확인한다.
- 최종 빌드에서 공개24개를 root가 다시 비교해 기존 계산 스타일·치수와 모두 일치했다. 독립 검토와 root 재실행의 실제 홈/투어→커뮤니티→뒤로가기 390/1280px 4개 왕복·162조건도 PASS. 초기 커뮤니티 CSS/규칙0→진입 CSS1개/385규칙, 복귀 제목·헤더 변화0px/넘침0. 차단한 프로모션/후기 API의 콘솔 오류와 미설명 오류0을 구분한다. 실제 커뮤니티 링크/모바일 Navigation 메뉴만 조작했고 주소를 강제로 바꾸어 클릭을 대신하지 않았다.
- 최종 초기 스타일 파일에 커뮤니티 선택자0, 빌드175개 JS/CSS의 신규 DEV fixture 표식0을 확인했다. `preview-ai-center`라는 기존 manifest/진단 분기 문자열이 남은 것과 DEV 하네스 코드가 배포된 것은 구분한다.

## 성능 판정과 남은 경계

- 최종 빌드도 같은 로컬 환경의 고정 LHCI0.15.1/Lighthouse12.6.1/Chrome152로 홈·투어·차량을 새 폴더에서 각각 1회 수집했다. 성능98/98/97, 접근성100/100/100, 전송577,329/650,041/602,342B, LCP1,001/1,004/1,069ms, 세 경로 TBT0/CLS0/실행 오류 없음이다. 이전 후보 대비 전송량은 각각5,170/5,170/5,183B 줄었다. 운영 속도 또는 반복 측정 통계로 확대하지 않는다.
- 동일 판정 기준의 최종 assert는 필수 오류13/경고10, exit1이다. 이전 후보의 오류10에 forced-reflow가 세 경로 추가되었다. 원본에는 출처 미상 `[unattributed]`의 레이아웃 시간72.541/55.114/55.238ms가 기록되어 있으므로 CSS 변경 때문 또는 측정 변동이라고 단정하지 않는다. 수집 성공·높은 종합 점수와 필수 판정 실패를 별도로 보고한다. 원본은 `tmp/lighthouse-final-20260908/.lighthouseci`에 보존한다.
- 이전 보고서의 요청 경로 실패는 문서→manifest 81/92/81ms이고 LCP 절감 추정0이다. 고정 Lighthouse12.6.1에 포함된 NetworkDependencyTree 구현은 경로 길이2 이상이면 시간 임계값 없이 실패로 표시한다. manifest 삭제나 불필요한 preload로 판정만 피하지 않았다.
- 초기 Firestore 코드 다운로드와 실제 게스트 DB 질의를 혼동하지 않는다. 회원 기능·로그인 복원·프로필/쿠폰/장바구니가 연결된 모듈 분리는 새 안전 설계 없이 실행하지 않았다. 인증/가격/보호 기준 변경 없음.
- 이번 조회에서 연결된 휴대폰0대. 실제 Android 로그인·기기 등록·백그라운드 알림·PC OFF·앱 업데이트 전환은 완료 증거가 없다. 기존 APK 설치/빌드/가짜 브라우저 성공으로 대신하지 않는다.
- 예약/문의 푸시 코드는 기본 OFF 준비이며, 실제 지정 기기·언어·보관 기간·발송 활성화 확인은 별도다. 수신 메일/WhatsApp/전체 업체 청구액의 서버 연결·고객 AI 자동응대·Hermes 이전 완료를 주장하지 않는다. 새 구독이나 키 발급/이전/권한 허용을 하지 않았다.

## 오답노트

1. 신뢰 도구의 최초 main 설치와 제품 품질 평가는 분리한다. 최초 설치 실패를 성능 통과로 읽지 말고, 설치 뒤 정확한 배포 head로 실제 수집까지 확인한다.
2. 부분 조회 실패가 배열 []로 내려와도 UI의 "0건/급한 일 없음"으로 확정하면 안 된다. 재시도 대기 없음도 발신 성공 증명이 아니다.
3. CSS 원문 보존만으로 캐스케이드 보존을 증명하지 못한다. 지연 CSS와 빌드된 Tailwind 반응형 규칙의 상대 순서, 별도 관리자 소비자까지 실제 화면으로 대조한다.
4. 테스트 fixture는 API의 ok/data 구조를 그대로 사용한다. 마우스 클릭 직후 focus()만으로 키보드 focus-visible 실패를 판정하지 말고 실제 키보드 조작을 한다. 긴 페이지 캡처의 sticky 헤더는 스크롤 애니메이션이 끝나기 전 잘못된 위치에 찍힐 수 있으므로 테스트에서 순간 스크롤/상단 위치를 확인하고 화면을 저장한다.

## 반영 상태

검사 도구 PR #1381만 머지했다. 이 제품 후속은 PR #1380에 묶어 최종 회귀·푸시·정확한 Preview 검사를 이어간다. 최종 원격 결과/운영 배포/열린 PR 여부는 PR 본문과 Brain 공유 인계 기록에 추가하며, 이 로컬 검증 기록을 운영 반영 완료로 읽지 않는다.
