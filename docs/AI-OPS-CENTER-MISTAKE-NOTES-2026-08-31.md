# AI 운영센터 오답노트 — 2026-08-31

- 2026-09-09 보조 메일: 이메일 채널 이름만으로 설정/상태/보관/알림을 찾으면 두 계정이 섞인다. 고정 계정 ID로 분리하고, 보조 계정은 전용 사용자 라벨의 ID·이름·유형 확인 뒤에만 목록을 읽는다. 라벨 없는 메시지는 헤더/미리보기 요청 전에 최소 메타데이터로 거른다. 받은편지함(INBOX)을 업무 라벨로 설정할 수 없게 한다.
- 보조 메일에는 답장 작성기와 전송 호출을 연결하지 않는다. 기존 계정 필드 없는 기본메일은 호환하되 알 수 없는 계정과 모순된 보조 답장 허용 응답은 거부한다. 외부 응답 배열의 null/undefined 원소를 키 계산 전에 검사해 화면 검증 함수가 예외를 던지지 않도록 한다. 실제 인증 연결과 합성 검사를 구분한다.

- 첫 전체 검사에서 기존 합성 받은함 상세가 새 답장 패널의 실인증 경로를 호출하는 회귀 5개를 잡았다. 미리보기에는 합성 답장 전송기가 있을 때만 패널을 렌더해 제품에서 격리를 복구했다. 기존 네트워크 0 검사·시간 초과 검사 30개와 실제 합성 브라우저 10개를 그대로 통과시켰으며 업로드 전 정상 훅이 차단했다.

- 2026-09-09 메일: 보관 형식을 v2로 바꿀 때 받은함만 검사하면 알림 후보와 추가 검증에 남은 양수 만료 조건이 새 문의를 전부 누락시킨다. 실제 수신 형식→상담→알림 장부까지 같은 자료로 왕복 검사한다.
- 발송 전 확정 실패와 요청 후 결과 불명확은 다르다. 전자만 새 사람 확인/실패 시도 ID로 1회 재시도하며, 후자는 자동 재전송하지 않는다. 회사 프로필/권한과 승인 시각을 Gmail POST 직전에 다시 확인한다.
- 수정 초안에 기존 요청 키를 재사용하면 본문 해시 충돌로 저장되지 않는다. 수정은 새 키, 같은 요청은 같은 키로 구분하고, POST 실패에도 장부가 진행됐을 수 있어 GET으로 다시 확인한다. 통신 불명확은 재조회 전 발송을 잠근다.
- 새 하네스의 Firebase 초기화 값 누락은 앱 기능 실패와 구분한다. 실제 키를 복사하지 않고 해당 개발 프로세스에만 합성 공개 설정을 넣어 외부 요청을 막고 검사한다. 메일 fixture도 고정 임의 ID가 아니라 실제 준비 함수가 만든 원본 식별자를 써야 한다.
- 성공 화면만 44px 검사하면 작성·확인 컨트롤은 이미 사라져 허수 통과한다. 발송 전 클릭 영역/키보드 이동도 검사하고 같은 상태 설명은 한 번만 표시한다. 합성 화면·업체 접수·실폰 수신을 각각 구분한다.

- 2026-09-09 보관: 상담 종료 후 30일과 수신한 지 30일을 혼동하지 않는다. 진행 중/증빙 보호/불명확 발송은 일괄 삭제하지 않고, 늦은 새 문의가 만료된 원문을 되살리지 않도록 이전 만료 구간을 유지한다. 원본 문의와 묶인 답장 사본은 같은 거래에서 확인·정리한다.
- 첫 전체 검사에서 기존 받은함 API 합성 DB가 v2 문의만 만들고 상담 case는 만들지 않아 2개가 차단됐다. 새 저장 형식의 전체 읽기 경로를 실제로 흉내 내도록 fixture를 맞추며, case 없는 원문까지 허용하도록 제품 검사를 느슨하게 하지 않는다.
- 초안은 최초 작성 후 최대 7일이다. 편집으로 기간을 늘리지 않으며 원본 허용기한이 짧아지면 초안 기한도 함께 줄여 장부 검증이 깨지지 않게 한다. 이미 발송된 이력은 7일 지난 초안으로 취급하지 않는다. 정리 조회/쓰기 실패를 성공으로 기록하지 않는다.
- 관리자 상태 변경은 거래 안의 마지막 읽기 뒤 실제 시각을 재확인한다. 화면 검증 함수의 boolean을 상세 객체로 쓰지 않으며, 다른 문의를 열었을 때 늦게 온 결과와 이전 확인 체크를 폐기한다. 409와 시간 초과는 재조회 안내로 구분한다.
- Playwright의 목록 출력도 기본 HTML 보고서를 바꿀 수 있다. 실제 검사는 --reporter=line을 쓰고 합성 390px/1280px·4개 언어·외부 요청 차단으로 클릭을 확인한다. 초기 잘못된 하네스 경로와 약한 테스트는 교체했고 Spark 초안의 타입 린트 위반도 수정했다.

- 2026-09-09: 제목/발신자 검색은 이미 불러온 요약에서만 수행한다. 검색어를 주소·저장소·분석 이벤트로 보내지 않고 본문을 자동 조회하지 않는다. 조건 변경 시 이전 상세 요청을 취소하고 페이지를 초기화한다. 로컬 선택 메뉴에도 명시적인 접근성 이름을 붙여 실제 브라우저의 이름 계산 차이를 검사한다.
- 조회 한도에 닿은 목록을 전체 대기 건수로 보지 않는다. 일부 원본은 정렬 조건 없이 조회하므로 모든 원본을 '최신순'이라고 안내하지 않는다. 실패/범위 미확인과 한도 도달을 분리한다.
- 캡틴시트 추가금 검사에서 줄을 넘는 정규식이 다음 줄의 같은 금액인 초과시간 요금을 읽고 통과했다. 정확한 상품·추가금 문장과 무료라는 모순 문구 부재를 함께 검사한다. 단위검사 중에는 실제 AI/결제를 호출하지 않는다.
- 병렬 구현 중 화면을 검사하면 페이지와 번역 모듈 사이의 저장 순서 때문에 일시적인 실행 오류가 생길 수 있다. 파일 안정화 뒤 새 브라우저 문맥에서 재검증하며 최초 실패도 기록한다.

- 2026-09-09: 발송 점유 저장의 응답이 유실되면 실제 저장 여부를 추측하지 않는다. 이미 남은 점유는 재발송 금지로 보존한다. 업체 접수 성공은 고객 배달/읽음 증명이 아니며, 승인 만료·STOP·동시 요청·저장 실패를 가짜 전송기로 따로 검사한다.
- P91은 정규식 안의 리터럴 백틱도 홀수 개면 미종결 문자열로 오탐한다. 허용 이메일 문자 집합을 바꾸거나 린트를 끄지 않고 동등한 문자 이스케이프로 표현하고 해당 주소가 여전히 승인되는 회귀를 추가한다.
- 정기검사 자료를 새로 조회했어도 검사 자체는 오래되거나 실패했을 수 있다. 조회 신선도와 실행 결과를 분리하고, 공용 GitHub 무인증 한도를 고려해 6개별 호출 대신 최신 정기 실행 100건을 한 번 조회한다. 조회 실패를 검사 0건/통과로 바꾸지 않는다.
- 웹채팅의 최신 시각은 기존 수신 경로가 갱신하는 lastMessageAt이다. 오래된 보조 밀리초 필드로 새 문의를 놓치지 않는다. 새 문의 충돌 시 초안은 남기되 새로고침 전 재전송을 잠근다. 같은 요청 재시도는 기존 영수증을 확인하고 새 메시지를 만들지 않는다.
- Spark CLI의 파일 쓰기 제한은 사용 한도 소진이 아니다. 같은 실패를 반복하거나 우회하지 않고, 도구 없는 작은 코드 작성에 사용한 뒤 주 담당이 적용·검증한다. 이 작업의 Playwright 주소 설정은 BASE_URL이며 다른 이름은 기본 5173에 붙어 허수 실패를 만든다.

- 로컬 전용 화면 검사도 공용 analytics-guard에서 test/expect를 가져온다. 개별 외부 차단기가 있어도 공용 검사 기록을 빠뜨리면 pre-push가 거부한다. 분석 URL은 공용 차단기로 넘겨 실제 차단 증거와 실패 판정을 유지한다.

- 2026-09-08: DB 자동응대 플래그 하나로 정상이라 표시하지 않는다. 예약 작업 환경·SMTP·접수확인 조건을 함께 검사하고 설정 준비/실제 수신·발송 증거를 분리한다. 채널별 미구현·읽기 전용·동의 세션 수신을 접힌 목록으로 표시하며 갱신 실패 때 지난 설정을 활성 상태로 보여주지 않는다.
- Telegram 재전송의 키는 원문 알림 ID가 아니라 봇 구분+update_id다. 같은 문의의 여러 정상 답장을 보존하고 동시 재시도는 고객 메시지와 세션을 같은 트랜잭션으로 한 번만 저장한다. 고객 저장 성공과 Telegram 확인 메시지 성공은 별개다.
- 웹채팅의 친근한 말투와 인간 사칭은 다르다. 실제로 없는 가이드 경력과 AI가 아니라는 주장을 넣지 않는다. 이 정적 프롬프트 검사는 실제 모델 답변의 품질 시험을 대신하지 않는다.

- 새 공개 경로는 App 라우트만 추가하면 운영 서버에서 404가 된다. Vercel의 정확한 SPA 경로 허용목록·검색 제외 헤더·seoRoutes도 함께 추가하고 기존 없는 경로 404는 유지한다. 전체 검사를 한꺼번에 과도하게 실행해 시간 초과가 나면 검사 생략/시간 제한 완화 대신 같은 전 범위를 제한된 작업 수로 재실행한다.
- 공개 동의 화면은 실제 앱의 배너·가입 팝업·하단 메뉴와 함께 점검한다. 토큰은 Tailwind에 실제 있는 `bg-bg-base`를 사용하고, hook이 붙이는 페이지 제목 브랜드를 중복하지 않는다. 접힌 안내/선택하지 않은 동의/언어 변경 초기화도 각각 확인한다.
- 관리자 제외 해제의 POST 성공 뒤 목록 누락을 성공으로 간주하지 않는다. 해당 번호의 닫힘 상태를 직접 다시 확인해야 완료를 표시한다. 변경 후 초점은 disabled/안내 DOM이 반영된 다음 복구한다.
- WhatsApp 시각은 초 단위라 START와 같은 초의 본문은 전후를 구별할 수 없다. `sourceAtMs > startedAtMs`만 허용하고 같은 묶음·후속 묶음 양쪽에서 저장 0건을 검증한다. 다음 초 문의는 정상 저장되는 대조 검사도 둔다.
- 같은 WhatsApp 번호 확인은 업무 대화 확인이 아니다. 원본 exact START 이전/닫힘/제외/만료 메시지는 저장하지 않고, AI로 본문을 먼저 분류하지 않는다. 제어문자 제거 뒤 START가 되는 입력은 거부하고 서명·계정 확인 뒤 원문 전체 일치로만 판단한다.
- 종료·제외 검사와 메시지 쓰기는 같은 트랜잭션에 둔다. STOP/관리자 종료의 수신 시각 장벽, 재시도 시 재시각 검사, 오래된 START/본문·동일 초·역순 묶음·제외 해제 후 재시작을 각각 역검증한다. START 중복으로 상담 시간을 늘리지 않는다.
- 개인정보 설정 준비와 수신 활성화는 별개다. 수신 OFF에서 제외 연락처를 먼저 등록할 수 있어야 한다. 제외 해제는 상담 재개가 아니며 새 START가 필요하다. 원문이 서버에 도착하지 않음, 과거 저장분 삭제, 실제 AI 응대까지 완료했다고 확대하지 않는다.
- 관리자 목록에 보호 모드 이전 WhatsApp 기록을 다시 노출하지 않는다. 목록/직접 ID 양쪽에서 세션 정책 증거를 검사한다. 해시 문서 ID는 익명화가 아니며 원문·본문·프로필·토큰이 오류나 로그에 들어가지 않게 한다.
- WhatsApp 서명은 원본 바이트로 확인한다. 일반 Vercel Node 함수에 Next.js bodyParser 설정이나 JSON 재직렬화를 적용하지 않는다. 정상 1,000건 묶음을 100건 제한으로 영구 거절하지 말고 크기·묶음·저장 왕복을 함께 제한한다.
- Gmail의 처음 목록을 다 읽기 전에 기준 변경 위치를 확보하고, 모든 페이지의 저장이 끝나야 위치를 옮긴다. INBOX 범위를 요청과 메시지에서 재확인하고 회사 계정·읽기 전용 권한·설정 변경·lease/fence를 따로 검사한다. Preview에는 실제 수신을 허용하지 않는다.
- 문의 목록에 본문을 싣지 않는다. 별도 상세 GET에서도 회사 계정·시작일·만료·식별자를 재검사한다. TTL 필드 준비와 실제 삭제 활성화는 다르며 설정 완료로 실제 수신 성공을 주장하지 않는다.
- 독립 받은함을 집계 로딩 전/후 서로 다른 위치에 넣으면 첫 진입에 두 번 연결된다. 같은 위치를 유지하고 uid 변경 때만 상태를 새로 만든다. 토큰 대기도 시간 제한 대상이며 닫은 뒤 온 본문과 다른 계정의 늦은 응답은 버린다.
- 이 레포 Vitest의 TSX 검사에는 React import가 필요하다. 새로운 독립 받은함의 요청은 전용 테스트에서 검증하고 원래 집계 요청의 정확한 호출 수 검사를 완화하지 않는다. 미연결 항목 3개 고정 검사는 실제 수신 상태 2개와 청구 미연결 1개의 새 표시 계약으로 바꾼다.

- 가로 메뉴의 native Tab은 일부만 보여도 추가 스크롤을 생략할 수 있다. 44px 실제 크기만 확인하지 말고 잘림을 제외한 노출 영역·정/역Tab·scrollLeft경계·초점 후 세로/hash불변을 실측한다. scrollIntoView는 문서 세로도 움직이므로 이 보완에는 nav.scrollLeft만 사용한다.
- 역할이 같은 상태 영역이 추가되면 이름 없는 status 검색이 모호해진다. 실제 갱신 상태에 네 언어 접근성 이름을 부여해 검사 대상을 좁히되, 새 비용 장애도 전체 "일부 갱신"에 반영한다. 비용 실패 때문에 정상 예약 수까지 불명확으로 바꾸지 않는다.
- 저장된 비용 기록의 빈 값/조회 실패/한도 도달과 실제 0원을 구분한다. 작은 양수를 집계 정밀도 아래에서 0으로 반올림해 정상 비용으로 표시하지 않는다. 조회 대기를 제한하고 늦은 실패도 처리한다. 기록된 추정액은 업체 실제 청구액이 아니다.
- 알림 설정 helper는 비밀값을 포함하므로 원문을 API 응답으로 펼치지 않는다. 허용된 상태 값만 새 객체로 반환하고, 설정 충족을 실제 수신 완료로 바꾸지 않는다.
- APK의 실제 package 버전명과 버전코드도 설정과 대조한다. 최신 소스와 유효한 서명만으로 다른 작업 폴더의 구버전 APK가 최신이라고 증명할 수 없다.
- Lighthouse가 대상 lazy 사진을 요청하지 않았으면 빈 이미지 audit를 압축 성공으로 보고하지 않는다. 동일 조건의 실제 이전/이후 요청을 확인하고 도구의 실행 오류·대상 미수집과 제품 성능 실패를 구분한다.
- 부분 조회 실패가 빈 배열로 내려와도 0건/급한 일 없음으로 확정하지 않는다. partialErrors와 sources.ok=false를 함께 확인하고 영향받는 출처만 확인 불가로 표시한다. 메일 재시도 대기 없음은 실행 성공·받은편지함 연결 증명이 아니다.
- CSS 원문을 그대로 옮겨도 Tailwind 반응형 유틸리티와 로딩 순서가 바뀌면 화면이 달라진다. 실제 PC에서 md:hidden 버튼이 되살아난 회귀를 복구하고 공개/작성/관리자 소비자와 다른 화면 왕복까지 비교한다. 기존 36/38px 관리자 버튼은 별도 44px 개선으로 구분한다.
- 보호 미리보기 검사 도구 최초 설치와 제품 성능 판정은 분리한다. main 설치 후 정확한 배포 head로 실제 3경로 수집까지 검증하며, 수집 성공을 필수 성능 판정 성공으로 보고하지 않는다.
- 가짜 API 응답도 실제 ok/data 계약을 맞춘다. 키보드 초점은 마우스 클릭 뒤 focus()만으로 판정하지 않고 Tab 조작으로 확인한다. 긴 페이지 사진은 스크롤 애니메이션을 끝내고 scrollY=0을 확인해 sticky 헤더 촬영 오류와 앱 오류를 구분한다.
- 제목 scrollHeight가 clientHeight보다 크다고 곧바로 글자 잘림은 아니다. 실제 Linux 제목에서 176/173으로 새 검사가 실패했지만 line-height:1.02의 기기 글꼴이 overflow:visible인 경우 줄 상자 밖까지 표시될 수 있다. 임계값을 늘리지 말고 텍스트 Range가 제목 자신 및 hidden/clip 조상의 실제 잘림 영역을 벗어나는지 검사한다. 보이는 돌출은 정상, 같은 글자의 hidden/clip은 실패하는 반례로 검증한다.
- 모바일 가로 넘침 검사에서 window.innerWidth는 넘친 자식 때문에 함께 커질 수 있다. 실제 Chromium 반례를 확인하고 document.documentElement.clientWidth를 기준으로 삼았다. 정상 Pixel 5 축척과 넘침 사례를 함께 검사한다.
- 웹폰트를 기기 글꼴로 바꾸면 Linux 기준 화면의 글자 폭·줄바꿈도 의도적으로 달라진다. 실패 이미지를 무조건 기준으로 덮지 말고 expected/actual/diff를 사람이 대조한다. 검토된 동일 Linux 실행의 한 장만 갱신하며 비교 1%는 유지하고, 잘린 캡처 밖의 제목 자체와 조상 상자의 overflow 잘림·가로 넘침도 DOM 검사로 잠근다. 제목 자체의 scrollHeight만으로 바깥 상자의 잘림까지 증명할 수는 없다.
- `picture { display: contents }` 아래 `source`가 grid 항목으로 올라와 사진 위에 빈 행을 만들 수 있다. source는 명시적으로 display:none 처리하고, srcSet만 바꾼 가짜 비교가 아니라 picture를 제거한 원래 img 구조와 카드 전체 높이·텍스트 위치를 비교한다. 실제 390px에서 재현 후 수정했다.
- 표시 문구를 접근성 이름으로 쓰도록 고친 뒤에도 제거한 aria-label을 찾는 정적 검사는 낡은 계약이다. 실제 로고 링크를 찾아 44px 기준을 계속 검사하고, 4언어 렌더 검사와 실제 브라우저 치수를 함께 유지한다.
- 검색창을 지연 로딩할 때 Suspense는 청크 실패를 잡지 않는다. 실패를 모의 주입해 기존 작성 화면이 살아 있는지 검사하고 검색 영역 안에서만 닫기·재시도 처리한다. 작은 성능 개선 때문에 작성 내용이 사라지면 안 된다.
- 닫힌 화면만 접근성 검사하면 공연 선택 뒤의 이름 없는 인원 버튼과 작은 승차장 버튼을 놓친다. 실제 선택 후 드러나는 컨트롤도 이름·44px·초점·기존 인원 범위와 예약 전달 값을 검사한다.
- PDF용 글꼴은 일반 화면을 막아 준비하지 않는다. 다운로드 때 실제 문자와 보통/굵은 두께를 명시적으로 불러온 후 fonts.ready를 확인하고, 실패·시간초과는 캡처 허용으로 해석하지 않는다.
- 원본 사진을 작은 카드에 그대로 쓰거나 CSS로 가린 이미지를 eager 로드하면 보이지 않는 바이트도 내려받는다. 실제 표시 크기·선택된 src·받은 바이트를 확인하고 로고용 자료가 모든 사진 목록을 공용 번들에 끌어오지 않게 나눈다.
- Lighthouse 카테고리 점수 통과와 개별 필수 검사 통과는 다르다. 로컬 미리보기와 운영 CDN의 속도도 그대로 차감하지 않고, 같은 환경의 수정 전/후 원본 보고서를 보존한다.
- PWA 등록은 렌더 중 시작되고 보호 훅의 mount는 이후 effect다. 첫 mount 이전 완료 신호를 버리면 이미 활성화된 업데이트가 계속 시간 초과될 수 있다. 첫 mount 전 준비 상태만 보존하고, 실제 해제 뒤 늦은 콜백은 계속 무시하는 두 경우를 나눠 검사한다.
- 서비스워커 활성화 요청 전 검사를 통과해도 실제 controlling 콜백 때는 입력/결제 상태가 달라질 수 있다. 새로고침 경로를 하나로 모으고 직전 상태·요청 시각·조작 순서를 다시 확인한다. 설치된 등록 라이브러리 원본을 실행해 다른 창 업데이트의 기본 강제 리로드도 회귀로 잠근다.
- 조회 원본 하나의 연결 누락이 전체 예약 작업을 매번 멈추면 정상 문의까지 영구 누락된다. 모호한 원본은 발송/건너뛰기 없이 해당 소스만 보류하고, 독립 원본과 이미 대기한 발송은 계속하는지 반복 실행으로 검사한다.
- 정확한 이어 읽기에는 같은 시각의 문서 ID까지 필요하다. 원본 ID를 전송 장부에 평문 복사하지 말고 별도 암호화 제어 상태로 보존한다. 키/기기/설정 변경·변조 때 자동 재초기화하면 과거 발송이나 누락이 생기므로 보류한다. OFF일 때 DB 0회면 보관기간 정리도 실행되지 않는다.
- 보호된 Preview 검사에서 PR 코드에 비밀값을 맡기지 않는다. 신뢰한 main의 도구와 고정 도구 버전, 같은 저장소/정확한 SHA/배포 호스트를 검증한다. main에 새 도구가 아직 없는 첫 PR은 승인된 머지 후 같은 Preview를 다시 검사하며, 인증 전 실패를 앱 점수 통과로 표시하지 않는다.
- React 렌더 중 ref.current를 읽어 설치 여부 prop으로 넘기면 현재 린트가 거부한다. 고정 초기값은 useState 초기화로 보존하고 최신 입력/결제 상태는 콜백 시점에 DOM에서 확인한다.
- 같은 자동화 원본이 상태 카드와 긴급 업무 목록에 따로 표시된다. 한곳만 이름을 고치면 다른 곳의 오해가 남는다. 실제 원본 식별값과 제목 형식을 확인해 표시만 맞추고 서버 수량·다음 행동·링크·다른 원본의 같은 문구는 보존한다.
- Lighthouse 작업이 성공이어도 실제 검사 URL이 Vercel 로그인 페이지일 수 있다. 인증 배선, 최종 URL, 원본 보고서 수, 실행 오류와 필수 assertion을 따로 확인하고 continue-on-error로 숨겨진 오류를 통과로 세지 않는다. assertion-results.json은 정상 전부 통과 시 빈 배열일 수 있으므로 파일 없음과 구분한다.
- email_retry는 받은편지함이 아니라 pending_email_retries 발신 재시도다. 상태 카드 제목만 보고 수신 문의 연결로 확대하지 않는다. API 비용 역시 기록된 Gemini 추정 사용액과 전체 업체 실제 청구액을 구분한다.
- 서비스워커 알림 클릭에서 첫 기존 창을 이동시키면 운영 알림이 작성/결제 화면을 덮을 수 있다. 새 업무 푸시를 연결하기 전에 대상 창 선택과 기존 작업 보호를 별도로 검사한다.
- 성공한 자료가 남아 있어도 다음 갱신 실패는 숨기지 않는다. `error && !data`는 오래된 예약을 '갱신 완료'로 오인시킨다. 첫 실패뿐 아니라 성공→실패→재시도 성공을 화면/시각/기존 행 보존까지 검사한다.
- 목록을 `slice(0, 10)` 또는 `slice(0, 30)`으로 줄일 때 더보기 없는 총 개수 배지는 업무 누락을 만든다. 마지막 묶음까지 열고, 표시 수/전체 수와 마지막 버튼 초점을 검사한다. 서버 원본 조회 한도와 화면 더보기는 별개다.
- 목록 기간은 화면 내부 state에만 두면 원본 링크에서 돌아올 때 초기화된다. URL에 기간만 저장하고 기존 쿼리/해시를 보존하며 replace로 변경한다. 고객 식별자나 업무 내용을 URL에 추가하지 않는다.
- '7일' 이름과 실제 +8일 계산을 혼동하지 않는다. 이 화면의 기존 서버 범위는 오늘 + 향후 7일이다. 표시만 명확히 하고 서버 집계/결제/예약 상태는 바꾸지 않는다. 한국 자정·마지막 포함일·제외일을 검사한다.
- 1280px 화면만 확인하면 1024px 두열 시작점에서 카드 내부 고정 4열이 겹치는 결함을 놓친다. 예약 카드의 이름/날짜는 두 칸, 상태는 다음 줄로 유지하고 실제 간격을 확인한다. 모바일 바깥 그리드에는 명시적 한 열과 최소 너비 0을 사용해 긴 영어의 가로 넘침을 막는다.
- 고정된 8월 31일 미리보기는 날짜가 지나면 오늘 예약은 빈 목록인데 요약만 남는 허수 검증을 만든다. 현재 한국 날짜의 가짜 행에서 요약도 계산하고 긴 목록/빈 상태/부분 실패/갱신 실패를 분리한다. 정적 실패 화면은 실제 통신 실패→재시도 시험의 대체가 아니다.
- 설치 프롬프트는 한 번만 사용할 수 있다. 클릭 직전 잠금과 공유 포인터 제거, 취소/예외 복구, unmount 후 상태 갱신 방지를 검사한다. accepted와 standalone은 APK 설치 완료나 업무 알림 실수신의 증거가 아니다.
- APK 빌드·서명·설치 `Success`·OS 실행 요청 `Status ok`는 실제 화면 성공을 뜻하지 않는다. ABH 2.7.3은 시작 중 `ManageDataLauncherActivity`를 참조하므로 비공개 등록과 고정 주소를 소스와 최종 병합 Manifest에서 각각 검사하고, 새 실행의 프로세스 오류·재진입·화면을 따로 확인한다. 이전 실행의 충돌 로그를 새 버전 오류로 재사용하지 않는다.
- 새 Android worktree에는 무시된 `local.properties`와 서명키 연결이 따라오지 않는다. 기존 보호 키에 연결한 로컬 설정과 Git 제외 상태를 먼저 확인하고 보호 실행기로 빌드한다. 설정 누락 때문에 키를 새로 만들거나 암호를 파일에 기록하지 않는다.
- `findByRole('status')`는 성공 DOM이 생긴 것만 기다리며, 이후 useEffect의 초점 이동 완료까지 보장하지 않는다. 키보드 접근성 검사는 초점 대상이 실제로 바뀔 때까지 `waitFor`로 확인한다. 기다림 없이 즉시 비교하면 전체 병렬 실행에서 간헐적으로 실패한다.
- Windows checkout의 CRLF와 apply_patch의 LF가 섞이면 정확한 문자열 치환으로 만든 회귀검사가 실제로 코드를 바꾸지 못할 수 있다. 테스트 입력은 LF로 정규화하되 운영 배포 후보의 원문 보존은 LF/CRLF 각각 바이트 단위로 검사한다.
- Firestore 규칙은 Vercel 웹 배포와 별개다. 승인된 일부 조건을 고칠 때 운영과 저장소 전체 규칙의 기존 차이를 먼저 확인하고, 관련 없는 차이를 함께 배포하지 않는다. 현재 운영 원문에 승인된 조건만 더한 후보를 검사하고 배포 직전 기존 ruleset·해시가 달라졌으면 중단한다.
- Firestore 조회 성공을 가짜 함수로만 검사하면 실제 보안 규칙 오류를 놓친다. `request.resource`는 쓰기 요청에만 있으므로 읽기 허용 조건에서 참조하면 본인 문서도 거부될 수 있다. 운영 규칙을 읽기 전용으로 대조하고 가상 요청으로 재현한 뒤, 보안 변경은 별도 승인받는다.
- 알림 권한 `granted`는 서버 기기 등록이나 실제 수신의 증거가 아니다. 기기 구독은 서버에서 따로 확인하고, 등록이 진행 중일 때 UI 타임아웃으로 재시도를 열지 않는다. 조회만 시간 제한과 이전 응답 무시를 적용한다.
- 설치 안내 컴포넌트가 standalone에서 일찍 반환하면 그 안의 알림 설정도 숨겨진다. 설치 전후 공통 알림 영역을 유지하고, 설치 여부만 확인했을 때는 최신 버전까지 확인한 것으로 표시하지 않는다.

- `data-cocotrip-navigation=reload`는 새로고침 방식만 뜻한다. 이것만으로 서비스워커 자동 업데이트 성공을 판정하지 말고, 수동 새로고침 없는 이전→새 배포 버전 전환과 실제 Android 실행을 별도로 확인한다.
- 새 worktree는 Vercel 프로젝트 연결 정보가 없다. 이 상태에서 `vercel curl`을 먼저 실행하면 폴더명으로 빈 프로젝트를 만들 수 있으므로, 기존 CocoTrip Vercel 프로젝트에 먼저 link한 뒤 조회한다.
- Windows 서명 보관 폴더의 상속을 끈 뒤 새 로컬 파일을 만들면 접근 규칙이 비어 실행이 막힐 수 있다. 키·암호·실행기 생성 후 파일별 ACL과 실제 release 명령을 다시 확인한다.
- `verify:prepush`의 실수 패턴 검사는 `origin/main..HEAD`만 보므로 변경이 아직 커밋 전이면 `no changes`로 건너뛴다. 커밋 뒤 같은 검사를 다시 실행해야 근거가 된다.
- Android 의존성은 표시 이름을 좌표로 추측하지 않는다. 공식 Google Maven의 실제 groupId·artifactId·version과 POM/AAR 존재 여부를 확인한 뒤 고정한다.
- `/admin`에서는 고객용 프로모션·쿠키·가입 모달·하단 탭·하단 여백을 모두 격리하고, 운영에 필요한 PWA 업데이트 알림만 유지한다.
- Firebase `getIdToken`을 변수로 떼어 호출하면 `User`의 `this`가 깨질 수 있다. 반드시 `user.getIdToken()`으로 호출한다.
- Windows에서 `.bat`를 `shell: true`로 실행하면 공백·메타문자 경로가 깨지거나 명령 주입 위험이 생긴다. Java 실행 파일에 wrapper·도구 인수를 배열로 직접 전달한다.
- `apksigner`의 모든 실패를 미서명으로 간주하지 않는다. ZIP 무결성·`aapt2` 패키지명·정확한 종료 코드와 `DOES NOT VERIFY` 메시지를 모두 확인하고 하나라도 다르면 실패한다.
- 개인용 sideload도 실제 서명키·SHA-256·공개 `assetlinks.json`·서명된 release APK가 모두 없으면 출시할 수 없다.
- `bookings`, `pending_bookings`, `mood_bookings`는 같은 표의 복사본이 아니다. 한 컬렉션만 읽고 “전체 예약”이라고 표시하지 않는다.
- 고객 이름·이메일·전화번호·여행일이 같다는 이유로 예약을 합치지 않는다. 확정된 입금 대기 문서와 정식 예약의 주문·예약·캡처 식별자가 정확히 일치할 때만 mirror로 정리한다.
- `P0` 순위 값은 숫자 `0`이다. `rank || fallback`을 쓰면 `0`이 fallback으로 바뀌어 가장 긴급한 일이 뒤로 밀린다. 키 존재 여부를 따로 검사한다.
- 과거 `decision_queue` 문서에 `status`가 없으면 정규화 결과는 `pending`이다. 처리 대상 여부도 원본 값이 아니라 이 정규화된 상태로 판단한다.
- 원본 조회 실패를 `0건`으로 바꾸지 않는다. 성공한 원본은 계속 보여주되 실패한 원본과 부분 집계 상태를 화면에 명시한다.
- 재시도 큐 읽기가 실패했을 때 빈 배열을 집계 함수에 넘겨 `정상 · 대기 없음`으로 표시하지 않는다. 해당 자동화 카드도 `상태 확인 실패`로 닫아 둔다.
- 통합 화면은 읽기 전용이다. 예약·결제·환불·배차 상태를 바꾸는 API를 연결하지 않고, 응답에도 고객 개인정보를 싣지 않는다.
- GitHub 정기검사 실행 이력이 아직 연결되지 않았다면 정상으로 추정하지 않는다. `미연동`으로 표시하고 실제 Actions 화면으로 연결한다.
