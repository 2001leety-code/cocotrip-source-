<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <queries>
    <package android:name="com.android.chrome" />
  </queries>
  <application
    android:allowBackup="false"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.CocoTripOwner"
    android:usesCleartextTraffic="false">
    <activity
      android:name=".OwnerLauncherActivity"
      android:exported="true"
      android:launchMode="singleTask"
      android:theme="@style/Theme.CocoTripOwner">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data
          android:scheme="https"
          android:host="cocotripkr.com"
          android:pathPrefix="/admin" />
      </intent-filter>
      <meta-data
        android:name="android.support.customtabs.trusted.DEFAULT_URL"
        android:value="https://cocotripkr.com/admin/ai-center" />
      <!-- App-only provider: Samsung Internet does not support notification delegation. -->
      <meta-data
        android:name="android.support.customtabs.trusted.LAUNCHING_BROWSER"
        android:value="com.android.chrome" />
      <meta-data
        android:name="android.support.customtabs.trusted.LAUNCHING_BROWSER_NAME"
        android:value="Chrome" />
      <meta-data
        android:name="android.support.customtabs.trusted.PROVIDER_AUTHORITY"
        android:value="com.cocotrip.owner.fileprovider" />
      <meta-data
        android:name="android.support.customtabs.trusted.FILE_PROVIDER_AUTHORITY"
        android:value="com.cocotrip.owner.fileprovider" />
    </activity>
    <!-- Android Browser Helper creates the site-settings shortcut during startup. -->
    <activity
      android:name="com.google.androidbrowserhelper.trusted.ManageDataLauncherActivity"
      android:exported="false">
      <meta-data
        android:name="android.support.customtabs.trusted.MANAGE_SPACE_URL"
        android:value="https://cocotripkr.com" />
    </activity>
    <!-- Only the verified browser can bind; ABH enforces its stored browser token. -->
    <service
      android:name="com.google.androidbrowserhelper.trusted.DelegationService"
      android:enabled="true"
      android:exported="true">
      <meta-data
        android:name="android.support.customtabs.trusted.SMALL_ICON"
        android:resource="@drawable/ic_owner_notification" />
      <intent-filter>
        <action android:name="android.support.customtabs.trusted.TRUSTED_WEB_ACTIVITY_SERVICE" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
    </service>
    <!-- ABH requests Android 13+ permission only after the website asks for it. -->
    <activity
      android:name="com.google.androidbrowserhelper.trusted.NotificationPermissionRequestActivity"
      android:exported="false"
      android:theme="@android:style/Theme.Translucent.NoTitleBar" />
    <meta-data
      android:name="asset_statements"
      android:resource="@string/asset_statements" />
  </application>
</manifest>
